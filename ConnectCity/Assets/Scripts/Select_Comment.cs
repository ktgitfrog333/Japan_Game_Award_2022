using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Select_Comment : MonoBehaviour
{
    public Text text;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void Comment(int stage)
    {
        if(stage == 1)
        {
            text.text = "ステージ1を選択します";
        }

        if(stage == 2)
        {
            text.text = "ステージ2を選択します";
        }

        if(stage == 3)
        {
            text.text = "ステージ3を選択します";
        }

        if(stage == 4)
        {
            text.text = "ステージ4を選択します";
        }

        if(stage == 5)
        {
            text.text = "ステージ5を選択します";
        }

        if(stage == 6)
        {
            text.text = "ステージ6を選択します";
        }

        if(stage == 7)
        {
            text.text = "ステージ7を選択します";
        }

        if(stage == 8)
        {
            text.text = "ステージ8を選択します";
        }

        if(stage == 9)
        {
            text.text = "ステージ9を選択します";
        }

        if(stage == 10)
        {
            text.text = "ステージ10を選択します";
        }

        if(stage == 11)
        {
            text.text = "ステージ11を選択します";
        }

        if(stage == 12)
        {
            text.text = "ステージ12を選択します";
        }

        if(stage == 13)
        {
            text.text = "ステージ13を選択します";
        }

        if(stage == 14)
        {
            text.text = "ステージ14を選択します";
        }

        if(stage == 15)
        {
            text.text = "ステージ15を選択します";
        }

        if(stage == 16)
        {
            text.text = "ステージ16を選択します";
        }

        if(stage == 17)
        {
            text.text = "ステージ17を選択します";
        }

        if(stage == 18)
        {
            text.text = "ステージ18を選択します";
        }

        if(stage == 19)
        {
            text.text = "ステージ19を選択します";
        }

        if(stage == 20)
        {
            text.text = "ステージ20を選択します";
        }

        if(stage == 21)
        {
            text.text = "ステージ21を選択します";
        }

        if(stage == 22)
        {
            text.text = "ステージ22を選択します";
        }

        if(stage == 23)
        {
            text.text = "ステージ23を選択します";
        }

        if(stage == 24)
        {
            text.text = "ステージ24を選択します";
        }

        if(stage == 25)
        {
            text.text = "ステージ25を選択します";
        }

        if(stage == 26)
        {
            text.text = "ステージ26を選択します";
        }

        if(stage == 27)
        {
            text.text = "ステージ27を選択します";
        }

        if(stage == 28)
        {
            text.text = "ステージ28を選択します";
        }

        if(stage == 29)
        {
            text.text = "ステージ29を選択します";
        }

        if(stage == 30)
        {
            text.text = "ステージ30を選択します";
        }
    }
}
